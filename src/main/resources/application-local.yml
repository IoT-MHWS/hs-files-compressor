server:
  port: 8769

app:
  paintings:
    repository:
      fs:
        mount: ${HS_FILES_PAINTINGS_VOLUME:./paintings}
    client:
      server-urls: ws://${HS_FILES_HOST:localhost}:${HS_FILES_PORT:8767}/ws


#logging:
#  level:
#    org:
#      springframework:
#        security: DEBUG
#        web: DEBUG
#      hibernate: ERROR
